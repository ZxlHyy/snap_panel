你是一名 Flutter 高级架构工程师，请设计一个高性能 Bottom Sheet 组件（SnapSheet）。

要求：
1. 不允许使用 Column + Expanded 作为主要布局结构（避免 flex overflow）
2. 使用 Stack + AnimatedContainer 作为核心结构
3. height 只能由单一变量控制（0~1 归一化）
4. 必须支持手势拖拽（onVerticalDragUpdate / End）
5. 必须支持 snap points（0%, 30%, 60%, 100%）
6. 不允许出现 RenderFlex overflow
7. 避免频繁 setState（优化性能）
8. 动画必须使用 AnimationController，不允许 implicit animation 滥用
9. controller 必须支持：
    - animateTo(double)
    - collapse()
    - expand()
10. 内部 content 必须支持任意 widget（ListView / Column / ScrollView）
11. 必须避免 layout rebuild 抖动

请先输出：
A. 组件架构设计图（文字说明）
B. 状态流转设计（state machine）
C. 组件拆分（class 列表）
然后再写代码实现

重点约束：
- 禁止使用 Expanded / Flexible 作为布局核心
- 禁止 Column 作为主容器
- 所有布局必须可预测高度
- 不允许出现 RenderFlex overflow
- 所有高度必须由 snap value * maxHeight + minHeight 控制

性能要求：
- 动画期间不允许 setState 每帧 rebuild 整棵 widget tree
- 使用 AnimatedBuilder 或 ValueNotifier
- drag 过程控制在 60fps
- 避免 build 方法中创建 controller / animation
- ListView 必须支持 controller 外传，避免嵌套滚动冲突

请设计一个 Flutter controller：
- 不允许 null crash
- 支持 safe attach/detach lifecycle
- 支持命令式 API（collapse/expand/animateTo）
- 内部状态必须可监听（ValueNotifier 或 Stream）


如果你发现任何 layout 可能导致 overflow，请优先重构结构，而不是用 Flexible / Expanded 修复。



请设计并实现一个 Flutter 高性能 Bottom Sheet 组件 SnapSheet（类似 iOS Maps bottom sheet）。

架构要求：
- 使用 Stack 作为根布局
- height 由 0~1 控制，不允许 Flex 布局作为核心结构
- 禁止 RenderFlex overflow
- 不使用 Column + Expanded 作为主要结构
- content 支持任意 widget（ListView / Column / ScrollView）

功能要求：
- 支持拖拽手势控制高度
- 支持 snap points（0, 0.3, 0.6, 1）
- 支持 velocity fling
- 支持 controller（animateTo / collapse / expand）
- 支持 backdrop

性能要求：
- 使用 AnimationController
- 拖拽过程不频繁 rebuild
- 避免 setState 每帧触发全树 rebuild
- 避免 layout jitter

工程要求：
- 代码结构清晰（controller / widget / state 分离）
- 不允许 hack layout 修 overflow
- 如果出现布局冲突必须重构结构

请先给设计说明，再给完整代码实现。